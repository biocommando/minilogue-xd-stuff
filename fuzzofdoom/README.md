## Fuzz of Doom
This is a heavy fuzz distortion that occupies the modulation effect slot. Modeled after my CLAP implementation at biocommando/FuzzOfDoom.

- TIME parameter controls the gain.
- DEPTH parameter controls the blend.

