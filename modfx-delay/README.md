## Delay
A very lo-fi mono delay effect for the modfx slot. Uses dynamic
delay buffer downsampling for longer delay times. At maximum samplerate
the delay time is 77 ms. The maximum delay time is 1543 ms with samplerate
of 2400 Hz. The bitdepth of the delay buffer is 12 bits.

Can be used for experimental sounds.
