## Drum machine
A simple drum machine intended for practice helper. Uses the same samples and sample playback mechanism as the drum-machine user oscillator, but only samples kick, snare, closed hihat and rimshot. Synced to tempo.

- TIME parameter selects the pattern.
- DEPTH parameter controls the mix volume. Setting depth to zero will reset the sequence so it can be used as a start/stop control.

